Running instructions:
— Run the VM, change the config.json file and restart the service;
— Execute ‘setup.sh’;
— Execute ‘dns_poison.py’.

The assumption is that the VM interface is vboxnet0 (info about this are in the report). If it’s not, the setup.sh script should be modified.
